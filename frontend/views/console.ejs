<style>
  html, body {
  height: 100%;
  margin: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: Arial, sans-serif;
}

/* Container for centering the form */
.container {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100%;
  width: 100%;
}

/* Form style */
.form {
  display: flex;
  flex-direction: column; /* Stack input and button vertically */
  align-items: center;
  padding: 20px;
  background-color: #f0f0f0;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

/* Style for the input field */
.input-field {
  padding: 10px;
  margin-bottom: 15px; /* Space between input and button */
  width: 200px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

/* Style for the button */
.submit-btn {
  padding: 10px 20px;
  background-color: #4CAF50;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 16px;
}

/* Button hover effect */
.submit-btn:hover {
  background-color: #45a049;
}


</style>
<html>
  <body>
    <div class="container">
<form class = form action="/input" method="POST">
    <h2>MyAttendance</h2>
    <input class=input-field type="number" name="code" maxlength="4" minlength="4" required>
    <h5><%= message %></h5>
    <button class=submit-btn type="submit">SUBMIT</button>
</form>
</div>
</body>
  </html>
